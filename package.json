{
  "name": "attendance-project",
  "version": "1.0.0",
  "description": "root",
  "private": true,
  "scripts": {
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix Attendance",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "emailjs": "^5.0.0",
    "react-chartjs-2": "^5.3.1",
    "recharts": "^2.12.7"
  }
}
